class Task {
  String? title;
  String? description;
  dynamic dueDate = DateTime.now();
  bool status = false;

  Task(this.title, this.description);
}
class newTask {
  String? title;
  String? description;
  dynamic dueDate = DateTime.now();
  bool status = false;

  newTask(this.title, this.description);
}

